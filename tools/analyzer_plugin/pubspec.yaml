name: dart_code_linter_analyzer_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 3.1.0-beta.4
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # TODO: Replace with the version when published.
  dart_code_linter:
    git: https://github.com/Mooyeee/dart-code-linter
